import express from 'express';


export function addItemsToCart(req: express.Request, res: express.Response) {
    // users to add items to their carts
}

export function updateItemsInCart(req: express.Request, res: express.Response) {
    // users to update items in their carts
}

export function deleteItemsFromCart(req: express.Request, res: express.Response) {
    // users to delete items from their carts
}

export function getCartByUserId(req: express.Request, res: express.Response) {
    // returns a single cart
}

